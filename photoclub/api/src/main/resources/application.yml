spring:
  application:
    name: api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_blog?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
  redis:
    host: 127.0.0.1
    password:
    jedis:
      pool:
        min-idle: 0
        max-idle: 20
        max-active: 100
server:
  port: 80

redis:
  host: 127.0.0.1
  password:
  jedis:
    pool:
      min-idle: 0
      max-idle: 20
      #            max-wait: 5000
      max-active: 100

  # Mybatis ����
mybatis:
  mapper-locations: classpath*:com/photo/dao/mapper/*Mapper.xml
  type-aliases-package: com.photo.dao.domain
  # Mybatis Map �ֶ�Ϊ�ղ�����
  configuration:
    call-setters-on-nulls: true


